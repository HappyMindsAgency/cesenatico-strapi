'use strict';

/**
 * classificazione-struttura controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::classificazione-struttura.classificazione-struttura');
