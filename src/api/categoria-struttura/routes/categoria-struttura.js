'use strict';

/**
 * categoria-struttura router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::categoria-struttura.categoria-struttura');
