'use strict';

/**
 * tipologia-struttura service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::tipologia-struttura.tipologia-struttura');
