'use strict';

/**
 * tipologia-struttura router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::tipologia-struttura.tipologia-struttura');
