'use strict';

/**
 * pagina-premium controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::pagina-premium.pagina-premium');
